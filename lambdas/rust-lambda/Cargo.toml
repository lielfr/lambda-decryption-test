[package]
name = "rust-lambda"
version = "0.1.0"
edition = "2024"

# Starting in Rust 1.62 you can use `cargo add` to add dependencies
# to your project.
#
# If you're using an older Rust version,
# download cargo-edit(https://github.com/killercup/cargo-edit#installation)
# to install the `add` subcommand.
#
# Running `cargo add DEPENDENCY_NAME` will
# add the latest version of a dependency to the list,
# and it will keep the alphabetic ordering for you.

[dependencies]
anyhow = "1.0.102"
aws-config = { version = "1.8.16", features = ["behavior-version-latest"] }
aws-sdk-s3 = { version = "1.132.0", features = ["behavior-version-latest"] }
aws-sdk-secretsmanager = "1.104.0"
aws_lambda_events = { version = "1.1.3", default-features = false, features = ["s3"] }

lambda_runtime = {version = "1.1.3", features = ["anyhow"]}
openssl = { version = "0.10.79", features = ["vendored"] }
tokio = { version = "1", features = ["macros"] }
